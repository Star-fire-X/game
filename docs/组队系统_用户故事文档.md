# 组队系统用户故事文档

## 1. 系统概述

组队系统是mir2游戏服务器的核心社交协作系统，负责管理玩家之间的组队合作、经验分享、组队聊天等功能。系统包含组队创建与管理、成员邀请与踢出、组队聊天、经验分配、跨服组队信息保存等子系统，为玩家提供丰富的团队协作玩法。

### 1.1 核心文件索引

| 文件路径 | 主要职责 | 关键行号 |
|---------|---------|---------|
| [ObjBase.pas](Source/M2Server/ObjBase.pas) | 组队属性定义、组队方法实现、组队聊天、经验分配 | 82, 283-291, 558, 710-715, 853-856, 2680-2691, 6960-7050, 15414-15551 |
| [Grobal2.pas](Source/Common/Grobal2.pas) | 组队消息常量定义(CM_/SM_/RM_) | 963, 1054, 1151-1159, 1274, 1345-1348, 1455, 1491, 1550 |
| [UsrEngn.pas](Source/M2Server/UsrEngn.pas) | 用户引擎、玩家管理、消息处理 | 145, 180 |
| [InterServerMsg.pas](Source/M2Server/InterServerMsg.pas) | 跨服组队信息结构、服务器切换时组队数据保存 | 34-50 |

### 1.2 系统架构图

```
组队系统
    │
    ├── 组队创建系统
    │       ├── 创建条件检查 (AllowGroup)
    │       ├── 队长设置 (GroupOwner)
    │       ├── 成员初始化 (GroupMembers)
    │       └── 成员上限检查 (GROUPMAX=11)
    │
    ├── 成员管理系统
    │       ├── 邀请加入 (ServerGetAddGroupMember)
    │       ├── 踢出成员 (ServerGetDelGroupMember)
    │       ├── 主动退出 (LeaveGroup)
    │       ├── 拒绝组队 (DenyGroup)
    │       └── 成员检查 (IsGroupMember)
    │
    ├── 组队解散系统
    │       ├── 解散条件 (CheckGroupValid)
    │       ├── 成员清理 (DelGroupMember)
    │       └── 状态重置 (GroupOwner=nil)
    │
    ├── 组队聊天系统
    │       ├── 组队消息广播 (GroupMsg)
    │       └── 消息类型 (RM_GROUPMESSAGE)
    │
    ├── 经验分配系统
    │       ├── 组队经验分享
    │       ├── 距离检查 (<=12格)
    │       └── 任务共享
    │
    ├── 成员同步系统
    │       ├── 成员列表刷新 (RefreshGroupMembers)
    │       ├── 位置同步 (SM_GROUPPOS)
    │       └── 状态同步
    │
    └── 跨服支持系统
            ├── 组队信息保存 (TServerShiftUserInfo)
            ├── 队长信息 (GroupOwner)
            └── 成员数组 (GroupMembers[0..8])
```

---

## 2. 核心数据结构

### 2.1 组队相关属性

**位置**: [ObjBase.pas:283-291](Source/M2Server/ObjBase.pas#L283-L291)

```pascal
TCreature = class
   // 组队相关变量
   GroupOwner: TCreature;      // 队长对象指针
   GroupMembers: TStringList;  // 队伍成员列表 (存储成员名和对象)
   AllowGroup: Boolean;        // 是否允许被邀请组队

   // 聊天控制
   BoHearWhisper: Boolean;     // 是否接收密语
   BoHearCry: Boolean;         // 是否接收喊话
   BoHearGuildMsg: Boolean;    // 是否接收行会消息
end;
```

### 2.2 组队常量定义

**位置**: [ObjBase.pas:82](Source/M2Server/ObjBase.pas#L82)

```pascal
GROUPMAX = 11;               // 组队最大人数
```

### 2.3 跨服组队信息结构

**位置**: [InterServerMsg.pas:34-50](Source/M2Server/InterServerMsg.pas#L34-L50)

```pascal
TServerShiftUserInfo = record
   UserName: string[14];                      // 用户名
   rcd: FDBRecord;                            // 角色数据记录
   Certification: integer;                    // 认证码
   GroupOwner: string[14];                    // 组队队长名称
   GroupMembers: array[0..8] of string[14];   // 组队成员名称数组（最多9人）
   BoHearCry: Boolean;                        // 是否接收喊话
   BoHearWhisper: Boolean;                    // 是否接收私聊
   BoHearGuildMsg: Boolean;                   // 是否接收行会消息
   // ... 其他字段
end;
```

### 2.4 攻击模式常量

**位置**: [Grobal2.pas:963](Source/Common/Grobal2.pas#L963)

```pascal
HAM_GROUP = 2;  // 编组模式，攻击编组外的任何人
```

---

## 3. 组队消息常量配置表

### 3.1 客户端消息 (CM_)

| 常量名 | 值 | 说明 | 位置 |
|-------|-----|------|------|
| CM_GROUPMODE | 1019 | 组队模式切换 | Grobal2.pas:1345 |
| CM_CREATEGROUP | 1020 | 创建队伍请求 | Grobal2.pas:1346 |
| CM_ADDGROUPMEMBER | 1021 | 添加队员请求 | Grobal2.pas:1347 |
| CM_DELGROUPMEMBER | 1022 | 移除队员请求 | Grobal2.pas:1348 |

### 3.2 服务器消息 (SM_)

| 常量名 | 值 | 说明 | 位置 |
|-------|-----|------|------|
| SM_GROUPMESSAGE | 101 | 组队聊天消息 | Grobal2.pas:1054 |
| SM_GROUPMODECHANGED | 659 | 组队模式改变通知 | Grobal2.pas:1151 |
| SM_CREATEGROUP_OK | 660 | 创建队伍成功 | Grobal2.pas:1152 |
| SM_CREATEGROUP_FAIL | 661 | 创建队伍失败 | Grobal2.pas:1153 |
| SM_GROUPADDMEM_OK | 662 | 添加队员成功 | Grobal2.pas:1154 |
| SM_GROUPDELMEM_OK | 663 | 移除队员成功 | Grobal2.pas:1155 |
| SM_GROUPADDMEM_FAIL | 664 | 添加队员失败 | Grobal2.pas:1156 |
| SM_GROUPDELMEM_FAIL | 665 | 移除队员失败 | Grobal2.pas:1157 |
| SM_GROUPCANCEL | 666 | 队伍解散通知 | Grobal2.pas:1158 |
| SM_GROUPMEMBERS | 667 | 队伍成员列表 | Grobal2.pas:1159 |
| SM_GROUPPOS | 1312 | 组队成员位置 | Grobal2.pas:1274 |

### 3.3 内部消息 (RM_)

| 常量名 | 值 | 说明 | 位置 |
|-------|-----|------|------|
| RM_GROUPMESSAGE | 10102 | 组队消息广播 | Grobal2.pas:1455 |
| RM_GROUPCANCEL | 10140 | 组队解散内部消息 | Grobal2.pas:1491 |
| RM_GROUPPOS | 11008 | 组队位置内部消息 | Grobal2.pas:1550 |

---

## 4. 组队功能对比表

### 4.1 组队操作对比

| 操作类型 | 触发方式 | 权限要求 | 处理函数 | 成功消息 | 失败消息 |
|---------|---------|---------|---------|---------|---------|
| 创建队伍 | CM_CREATEGROUP | 无队伍 | ServerGetCreateGroup | SM_CREATEGROUP_OK | SM_CREATEGROUP_FAIL |
| 添加成员 | CM_ADDGROUPMEMBER | 队长 | ServerGetAddGroupMember | SM_GROUPADDMEM_OK | SM_GROUPADDMEM_FAIL |
| 移除成员 | CM_DELGROUPMEMBER | 队长 | ServerGetDelGroupMember | SM_GROUPDELMEM_OK | SM_GROUPDELMEM_FAIL |
| 主动退出 | LeaveGroup | 队员 | LeaveGroup | - | - |
| 拒绝组队 | DenyGroup | 队员 | DenyGroup | - | - |

### 4.2 创建队伍失败错误码

**位置**: [ObjBase.pas:15436-15470](Source/M2Server/ObjBase.pas#L15436-L15470)

| 错误码 | 含义 | 说明 |
|-------|------|------|
| -1 | 已有队伍 | 创建者已经在其他队伍中 |
| -2 | 目标无效 | 目标玩家不存在或为自己或未登录 |
| -3 | 目标已有队伍 | 目标玩家已经在其他队伍中 |
| -4 | 目标拒绝组队 | 目标玩家设置了不允许组队 |

### 4.3 添加成员失败错误码

**位置**: [ObjBase.pas:15474-15527](Source/M2Server/ObjBase.pas#L15474-L15527)

| 错误码 | 含义 | 说明 |
|-------|------|------|
| -1 | 非队长 | 只有队长才能添加成员 |
| -2 | 目标无效 | 目标玩家不存在或为自己或未登录 |
| -3 | 目标已有队伍 | 目标玩家已经在队伍中 |
| -4 | 目标拒绝组队 | 目标玩家设置了不允许组队 |
| -5 | 队伍已满 | 队伍人数已达上限(GROUPMAX=11) |

### 4.4 删除成员失败错误码

**位置**: [ObjBase.pas:15531-15551](Source/M2Server/ObjBase.pas#L15531-L15551)

| 错误码 | 含义 | 说明 |
|-------|------|------|
| -1 | 非队长 | 只有队长才能移除成员 |
| -2 | 目标无效 | 目标玩家不存在 |
| -3 | 非队伍成员 | 目标玩家不在队伍中 |

---

## 5. Epic级别用户故事

### Epic 1: 组队创建系统

**描述**: 作为游戏服务器，我需要提供组队创建功能，让玩家能够建立自己的队伍并成为队长。

**核心文件**: [ObjBase.pas:15436-15470](Source/M2Server/ObjBase.pas#L15436-L15470)

**关键功能**:
- 创建条件验证(无现有队伍)
- 目标玩家验证(存在、在线、允许组队)
- 队长初始化(GroupOwner=self)
- 成员列表初始化(GroupMembers)
- 成员列表刷新(RefreshGroupMembers)

---

### Epic 2: 成员管理系统

**描述**: 作为组队系统，我需要管理队伍成员的加入、退出、踢出，维护成员列表的完整性。

**核心文件**: [ObjBase.pas:15474-15551](Source/M2Server/ObjBase.pas#L15474-L15551), [ObjBase.pas:6994-7050](Source/M2Server/ObjBase.pas#L6994-L7050)

**关键功能**:
- 成员加入(EnterGroup)
- 成员退出(LeaveGroup)
- 踢出成员(DelGroupMember)
- 成员检查(IsGroupMember)
- 人数上限检查(GROUPMAX=11)

---

### Epic 3: 组队解散系统

**描述**: 作为组队系统，我需要在特定条件下自动解散队伍，清理成员状态。

**核心文件**: [ObjBase.pas:6978-6991](Source/M2Server/ObjBase.pas#L6978-L6991)

**关键功能**:
- 解散条件检查(成员<=1人)
- 成员清理(GroupMembers.Clear)
- 状态重置(GroupOwner=nil)
- 解散通知(GroupMsg)

---

### Epic 4: 组队聊天系统

**描述**: 作为组队系统，我需要支持队伍内部的聊天频道，让队员之间可以私密交流。

**核心文件**: [ObjBase.pas:2680-2691](Source/M2Server/ObjBase.pas#L2680-L2691)

**关键功能**:
- 组队消息广播(GroupMsg)
- 消息类型(RM_GROUPMESSAGE)
- 遍历队伍成员发送
- 消息格式(玩家名: 消息)

---

### Epic 5: 经验分配系统

**描述**: 作为组队系统，我需要支持组队经验分享机制，让队员在击杀怪物时共享任务进度。

**核心文件**: [ObjBase.pas:3042-3057](Source/M2Server/ObjBase.pas#L3042-L3057)

**关键功能**:
- 组队任务共享
- 距离检查(<=12格)
- 同地图检查
- 存活状态检查

---

### Epic 6: 跨服组队支持

**描述**: 作为组队系统，我需要在玩家跨服切换时保存和恢复组队信息。

**核心文件**: [InterServerMsg.pas:34-50](Source/M2Server/InterServerMsg.pas#L34-L50)

**关键功能**:
- 队长信息保存(GroupOwner)
- 成员数组保存(GroupMembers[0..8])
- 跨服数据传输
- 组队信息恢复

---

## 6. 详细用户故事

### US-GROUP-001: 创建队伍

**用户故事**: 作为玩家，我需要能够创建队伍，邀请另一名玩家加入并成为队长。

**优先级**: P0 (核心功能)
**复杂度**: 中等

**验收标准**:
1. 玩家未加入任何队伍
2. 目标玩家存在且在线
3. 目标玩家未加入其他队伍
4. 目标玩家允许组队(AllowGroup=TRUE)
5. 创建成功后自动成为队长

**技术实现要点**:
- 处理函数: [ObjBase.pas:15436-15470](Source/M2Server/ObjBase.pas#L15436-L15470)
- 客户端消息: CM_CREATEGROUP
- 成功响应: SM_CREATEGROUP_OK
```pascal
// 创建条件检查
if GroupOwner <> nil then Exit;           // 已有队伍
if (who = nil) or (who = self) then Exit; // 目标无效
if who.GroupOwner <> nil then Exit;       // 目标已有队伍
if not who.AllowGroup then Exit;          // 目标拒绝组队
```

---

### US-GROUP-002: 添加队员

**用户故事**: 作为队长，我需要能够邀请其他玩家加入我的队伍。

**优先级**: P0 (核心功能)
**复杂度**: 中等

**验收标准**:
1. 操作者是队长(GroupOwner=self)
2. 队伍人数未达上限(GROUPMAX=11)
3. 目标玩家存在且在线
4. 目标玩家未加入其他队伍
5. 目标玩家允许组队

**技术实现要点**:
- 处理函数: [ObjBase.pas:15474-15527](Source/M2Server/ObjBase.pas#L15474-L15527)
- 客户端消息: CM_ADDGROUPMEMBER
- 成功响应: SM_GROUPADDMEM_OK
```pascal
if GroupOwner <> self then Exit;          // 非队长
if GroupMembers.Count >= GROUPMAX then Exit; // 队伍已满
```

---

### US-GROUP-003: 踢出队员

**用户故事**: 作为队长，我需要能够将队员从队伍中移除。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 操作者是队长
2. 目标玩家在队伍中
3. 踢出后更新所有成员的队伍列表

**技术实现要点**:
- 处理函数: [ObjBase.pas:15531-15551](Source/M2Server/ObjBase.pas#L15531-L15551)
- 客户端消息: CM_DELGROUPMEMBER
- 成功响应: SM_GROUPDELMEM_OK

---

### US-GROUP-004: 主动退出队伍

**用户故事**: 作为队员，我需要能够主动退出当前队伍。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 玩家在队伍中
2. 退出后通知其他队员
3. 更新队伍成员列表

**技术实现要点**:
- 处理函数: [ObjBase.pas:7034-7040](Source/M2Server/ObjBase.pas#L7034-L7040)
```pascal
procedure TCreature.LeaveGroup;
begin
   GroupMsg (UserName + '离开了组队。');
   GroupOwner := nil;
end;
```

---

### US-GROUP-005: 拒绝组队邀请

**用户故事**: 作为玩家，我需要能够设置拒绝组队邀请，防止被打扰。

**优先级**: P1 (重要功能)
**复杂度**: 低

**验收标准**:
1. 设置AllowGroup=FALSE后不能被邀请
2. 邀请者收到失败消息(错误码-4)

**技术实现要点**:
- 属性: AllowGroup: Boolean
- 处理函数: [ObjBase.pas:7043-7050](Source/M2Server/ObjBase.pas#L7043-L7050)

---

### US-GROUP-006: 队伍自动解散

**用户故事**: 作为组队系统，当队伍成员只剩1人时需要自动解散队伍。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 成员数<=1时触发解散
2. 通知剩余成员"你的组队已解散"
3. 清空成员列表和队长指针

**技术实现要点**:
- 处理函数: [ObjBase.pas:6978-6991](Source/M2Server/ObjBase.pas#L6978-L6991)
```pascal
function TCreature.CheckGroupValid: Boolean;
begin
   Result := TRUE;
   if GroupMembers.Count <= 1 then begin
      GroupMsg ('你的组队已解散。');
      GroupMembers.Clear;
      GroupOwner := nil;
      Result := FALSE;
   end;
end;
```

---

### US-GROUP-007: 组队聊天

**用户故事**: 作为队员，我需要能够在组队频道发送消息，只有队友能看到。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 消息只发送给队伍成员
2. 消息格式包含发送者名称
3. 使用`~消息`触发组队聊天

**技术实现要点**:
- 处理函数: [ObjBase.pas:2680-2691](Source/M2Server/ObjBase.pas#L2680-L2691)
- 消息类型: RM_GROUPMESSAGE
```pascal
procedure TCreature.GroupMsg (str: string);
var i: integer;
begin
   if GroupOwner <> nil then begin
      for i:=0 to GroupOwner.GroupMembers.Count-1 do begin
         TCreature(GroupOwner.GroupMembers.Objects[i]).SendMsg (
            self, RM_GROUPMESSAGE, 0, 0, 0, 0, '-' + str);
      end;
   end;
end;
```

---

### US-GROUP-008: 刷新成员列表

**用户故事**: 作为组队系统，当成员变更时需要刷新所有成员的队伍列表显示。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 成员加入/退出时触发刷新
2. 所有成员收到最新成员列表
3. 列表格式：成员名/成员名/...

**技术实现要点**:
- 处理函数: [ObjBase.pas:15414-15432](Source/M2Server/ObjBase.pas#L15414-L15432)
- 消息类型: SM_GROUPMEMBERS
```pascal
procedure TUserHuman.RefreshGroupMembers;
var i: integer; data: string;
begin
   data := '';
   for i:=0 to GroupMembers.Count-1 do begin
      cret := TCreature (GroupMembers.Objects[i]);
      data := data + cret.UserName + '/';
   end;
   for i:=0 to GroupMembers.Count-1 do begin
      hum := TUserHuman (GroupMembers.Objects[i]);
      hum.SendDefMessage (SM_GROUPMEMBERS, 0, 0, 0, 0, data);
   end;
end;
```

---

### US-GROUP-009: 检查成员身份

**用户故事**: 作为组队系统，我需要能够检查某玩家是否是队伍成员。

**优先级**: P1 (重要功能)
**复杂度**: 低

**验收标准**:
1. 遍历队伍成员列表进行匹配
2. 返回布尔值表示是否是成员

**技术实现要点**:
- 处理函数: [ObjBase.pas:6960-6975](Source/M2Server/ObjBase.pas#L6960-L6975)
```pascal
function TCreature.IsGroupMember (cret: TCreature): Boolean;
begin
   Result := FALSE;
   if GroupOwner <> nil then begin
      for i:=0 to GroupOwner.GroupMembers.Count-1 do begin
         if GroupOwner.GroupMembers.Objects[i] = cret then begin
            Result := TRUE;
            break;
         end;
      end;
   end;
end;
```

---

### US-GROUP-010: 组队任务共享

**用户故事**: 作为队员，击杀怪物时队伍内符合条件的成员应共享任务进度。

**优先级**: P1 (重要功能)
**复杂度**: 中等

**验收标准**:
1. 队员在同一地图
2. 队员距离<=12格
3. 队员未死亡
4. 触发地图任务检查

**技术实现要点**:
- 处理位置: [ObjBase.pas:3042-3057](Source/M2Server/ObjBase.pas#L3042-L3057)
```pascal
if ExpHiter.GroupOwner <> nil then begin
   for i:=0 to ExpHiter.GroupOwner.GroupMembers.Count-1 do begin
      cret := TCreature(ExpHiter.GroupOwner.GroupMembers.Objects[i]);
      if not cret.Death and
        (ExpHiter.PEnvir = cret.PEnvir) and
        (abs(ExpHiter.CX-cret.CX) <= 12) and
        (abs(ExpHiter.CY-cret.CY) <= 12)
      then begin
         // 触发任务
      end;
   end;
end;
```

---

### US-GROUP-011: 队长下线处理

**用户故事**: 作为组队系统，当队长下线或死亡时需要正确处理队伍状态。

**优先级**: P0 (核心功能)
**复杂度**: 中等

**验收标准**:
1. 检测队长死亡或变成幽灵状态
2. 清除队员的队长引用
3. 队伍自动解散或转移队长

**技术实现要点**:
- 处理位置: [ObjBase.pas:9064-9073](Source/M2Server/ObjBase.pas#L9064-L9073)
```pascal
if GroupOwner <> nil then
   if GroupOwner.Death or GroupOwner.BoGhost then begin
      GroupOwner := nil;
   end;
if GroupOwner = self then
   for i:=GroupMembers.Count-1 downto 0 do begin
      cret := TCreature(GroupOwner.GroupMembers.Objects[i]);
      if cret.Death or cret.BoGhost then
         GroupMembers.Delete (i);
   end;
```

---

### US-GROUP-012: 玩家下线清理

**用户故事**: 作为组队系统，当玩家下线时需要将其从队伍中移除。

**优先级**: P0 (核心功能)
**复杂度**: 低

**验收标准**:
1. 玩家下线时自动退出队伍
2. 通知其他队员
3. 检查队伍是否需要解散

**技术实现要点**:
- 处理位置: [ObjBase.pas:10213-10214](Source/M2Server/ObjBase.pas#L10213-L10214)
```pascal
if GroupOwner <> nil then
   GroupOwner.DelGroupMember (self);
```

---

### US-GROUP-013: 跨服组队信息保存

**用户故事**: 作为组队系统，当玩家跨服切换时需要保存组队信息。

**优先级**: P1 (重要功能)
**复杂度**: 中等

**验收标准**:
1. 保存队长名称
2. 保存成员名称数组(最多9人)
3. 数据随玩家传输到目标服务器

**技术实现要点**:
- 数据结构: [InterServerMsg.pas:38-39](Source/M2Server/InterServerMsg.pas#L38-L39)
```pascal
GroupOwner: string[14];                    // 组队队长
GroupMembers: array[0..8] of string[14];   // 组队成员
```

---

### US-GROUP-014: 编组攻击模式

**用户故事**: 作为玩家，我需要能够设置编组攻击模式，只攻击队伍外的玩家。

**优先级**: P2 (次要功能)
**复杂度**: 低

**验收标准**:
1. 设置HAM_GROUP模式后不攻击队友
2. 可以攻击队伍外的任何人

**技术实现要点**:
- 常量: HAM_GROUP = 2 ([Grobal2.pas:963](Source/Common/Grobal2.pas#L963))
- 攻击模式检查时判断IsGroupMember

---

### US-GROUP-015: 队长召唤队员

**用户故事**: 作为队长，我需要能够使用天地合一技能召唤队员到身边。

**优先级**: P2 (次要功能)
**复杂度**: 中等

**验收标准**:
1. 队长使用天地合一技能
2. 遍历队员检查BoEnableRecall
3. 允许召唤的队员传送到队长身边

**技术实现要点**:
- 处理位置: [ObjBase.pas:12603-12608](Source/M2Server/ObjBase.pas#L12603-L12608)
```pascal
if GroupOwner = self then begin
   for i:=1 to GroupMembers.Count-1 do begin
      if TUserHuman(GroupOwner.GroupMembers.Objects[i]).BoEnableRecall then
         CmdRecallMan (GroupMembers[i])
      else
         SysMsg (GroupMembers[i] + '无法被召唤', 0);
   end;
end;
```

---

### US-GROUP-016: 组队成员位置同步

**用户故事**: 作为组队系统，需要定期同步队员位置信息。

**优先级**: P2 (次要功能)
**复杂度**: 低

**验收标准**:
1. 定期发送队员位置信息
2. 客户端可显示队员在小地图上的位置

**技术实现要点**:
- 消息类型: SM_GROUPPOS, RM_GROUPPOS
- 位置: [Grobal2.pas:1274](Source/Common/Grobal2.pas#L1274), [Grobal2.pas:1550](Source/Common/Grobal2.pas#L1550)

---

### US-GROUP-017: 加入队伍通知

**用户故事**: 作为队员，加入队伍时应通知所有队友。

**优先级**: P1 (重要功能)
**复杂度**: 低

**验收标准**:
1. 新成员加入时广播消息
2. 消息格式："XXX加入了组队"

**技术实现要点**:
- 处理函数: [ObjBase.pas:7026-7031](Source/M2Server/ObjBase.pas#L7026-L7031)
```pascal
procedure TCreature.EnterGroup (gowner: TCreature);
begin
   GroupOwner := gowner;
   GroupMsg (UserName + '加入了组队。');
end;
```

---

### US-GROUP-018: 重复成员检查

**用户故事**: 作为组队系统，添加成员时需要检查是否已在队伍中。

**优先级**: P1 (重要功能)
**复杂度**: 低

**验收标准**:
1. 遍历成员列表检查重复
2. 重复时返回错误码-3

**技术实现要点**:
- 处理位置: [ObjBase.pas:15500-15513](Source/M2Server/ObjBase.pas#L15500-L15513)
```pascal
for i := 0 to GroupMembers.Count - 1 do begin
   if CompareText(TCreature(GroupMembers.Objects[i]).UserName,
                  who.UserName) = 0 then begin
      SendDefMessage(SM_GROUPADDMEM_FAIL, -3, 0, 0, 0, '');
      exit;
   end;
end;
```

---

### US-GROUP-019: 空成员检查

**用户故事**: 作为组队系统，需要处理成员列表中的空对象。

**优先级**: P1 (重要功能)
**复杂度**: 低

**验收标准**:
1. 检查成员对象是否为nil
2. 记录错误日志
3. 跳过空对象继续处理

**技术实现要点**:
- 处理位置: [ObjBase.pas:15502-15505](Source/M2Server/ObjBase.pas#L15502-L15505)
```pascal
if (GroupMembers.Objects[i] = nil) then begin
   MainOutMessage('ERROR: GROUP MEMBER IS NIL');
end
```

---

### US-GROUP-020: 队长解散队伍

**用户故事**: 作为队长，我需要能够主动解散整个队伍。

**优先级**: P1 (重要功能)
**复杂度**: 中等

**验收标准**:
1. 队长调用DelGroupMember(self)
2. 所有队员收到离开通知
3. 清空成员列表

**技术实现要点**:
- 处理函数: [ObjBase.pas:7011-7013](Source/M2Server/ObjBase.pas#L7011-L7013)
```pascal
for i:=GroupMembers.Count-1 downto 0 do begin
   TCreature (GroupMembers.Objects[i]).LeaveGroup;
   GroupMembers.Delete (i);
end;
```

---

## 7. 组队配置表

### 7.1 基础配置

| 配置项 | 值 | 说明 | 位置 |
|-------|-----|------|------|
| GROUPMAX | 11 | 组队最大人数 | ObjBase.pas:82 |
| 任务共享距离 | 12格 | 组队任务共享的最大距离 | ObjBase.pas:3048 |
| 跨服成员上限 | 9人 | 跨服传输时保存的成员数 | InterServerMsg.pas:39 |

### 7.2 默认属性值

| 属性 | 默认值 | 说明 |
|-----|-------|------|
| AllowGroup | TRUE | 默认允许被邀请组队 |
| GroupOwner | nil | 默认无队伍 |
| GroupMembers | 空列表 | 默认无成员 |

---

## 8. 边界场景处理表

| 场景 | 处理方式 | 代码位置 |
|-----|---------|---------|
| 队长下线 | 清除队员的GroupOwner引用 | ObjBase.pas:9064-9066 |
| 队员下线 | 从成员列表中移除 | ObjBase.pas:10213-10214 |
| 队长死亡 | 检测Death/BoGhost状态，清除引用 | ObjBase.pas:9064-9066 |
| 队员死亡 | 从成员列表中移除 | ObjBase.pas:9070-9073 |
| 成员对象为nil | 记录错误日志，跳过处理 | ObjBase.pas:15502-15505 |
| 队伍人数<=1 | 自动解散队伍 | ObjBase.pas:6982-6985 |
| 队伍已满 | 返回错误码-5 | ObjBase.pas:15484-15486 |
| 目标已有队伍 | 返回错误码-3 | ObjBase.pas:15453-15455 |
| 目标拒绝组队 | 返回错误码-4 | ObjBase.pas:15457-15459 |
| 重复添加成员 | 返回错误码-3 | ObjBase.pas:15508-15510 |
| 非队长操作 | 返回错误码-1 | ObjBase.pas:15480-15482 |

---

## 9. 核心方法索引表

| 方法名 | 功能 | 位置 |
|-------|------|------|
| GroupMsg | 组队消息广播 | ObjBase.pas:2680-2691 |
| IsGroupMember | 检查是否是队伍成员 | ObjBase.pas:6960-6975 |
| CheckGroupValid | 检查队伍是否有效 | ObjBase.pas:6978-6991 |
| DelGroupMember | 删除队伍成员 | ObjBase.pas:6994-7019 |
| EnterGroup | 加入队伍 | ObjBase.pas:7026-7031 |
| LeaveGroup | 离开队伍 | ObjBase.pas:7034-7040 |
| DenyGroup | 拒绝组队 | ObjBase.pas:7043-7050 |
| RefreshGroupMembers | 刷新队伍成员列表 | ObjBase.pas:15414-15432 |
| ServerGetCreateGroup | 处理创建队伍请求 | ObjBase.pas:15436-15470 |
| ServerGetAddGroupMember | 处理添加队员请求 | ObjBase.pas:15474-15527 |
| ServerGetDelGroupMember | 处理删除队员请求 | ObjBase.pas:15531-15551 |

