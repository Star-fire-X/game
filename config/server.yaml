# ==========================================
# 废弃通知 (DEPRECATED)
# ==========================================
# 本文件为旧通用服务器配置，已在双进程架构迁移中废弃。
# 当前生产环境使用 config/logic.yaml 和 config/gateway.yaml。
# 保留此文件仅作历史参考。
# ==========================================

server:
  id: 1
  name: "Mir2-Server-1"
  bind_ip: "0.0.0.0"
  port: 7000
  metrics_port: 9090
  io_threads: 4
  max_connections: 5000
  tick_interval_ms: 50

database:
  host: "127.0.0.1"
  port: 5432
  user: "mir2"
  password: "your_password_here"
  database: "mir2_game"
  pool_size: 10

log:
  level: "info"
  path: "logs"
  max_size_mb: 100
  max_files: 10

ecs:
  world_registry_reserve: 1000
